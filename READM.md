# QA Test (Python + Selenium + Pytest)

Автотесты для `qa-test.html` (локальная страница) на Python.

## Установка
```bash
python -m venv venv
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt
